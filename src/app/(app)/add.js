import { View } from "react-native";
import { Text } from "react-native-paper";

export default function AddPage() {
    return (
        <View>
            <Text>Add Page</Text>
        </View>
    )
}